/**
 *
 * this module is used to generate a browserify bundle during build time
 * the require() chain is statically analyzed by browserify to collect all
 * the needed modules.
 *
 */
require('./nb_console');
